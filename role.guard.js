/*
 * 守卫
 */
module.exports = function (creep, rampart) {
    if (creep_in_rampart) {
        if (find_army) {
            // rangeAttack
        }
    } else {
        creep.moveTo(rampart);
    }
}
